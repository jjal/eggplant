<% provide(:title, "All employees") %>
<% provide(:nav_tab, "employees") %>
<h1>All employees</h1>
<%= will_paginate %>
<table class="table table-striped table-condensed">
<th>Name</th>
<th>Pay Rate</th>
<th></th>
<tbody>
  <%= render @employees %>
</tbody>
</table>
<%= will_paginate %>
<br />

<%= button_to "New Employee", new_employee_path, method: :get, class: "btn btn-large btn-primary" %>
